---
import { Image } from "astro:assets";
import profile from "../assets/profile.webp";

const phrases = [
  { sentence: "Desarrollo de software eficiente", emoji: "💻" },
  { sentence: "Integración de hardware y software", emoji: "🔧" },
  { sentence: "Simulación de redes", emoji: "🖥️" },
  { sentence: "Gestión y modelado de Bases de Datos", emoji: "🗄️" },
  { sentence: "Asistencia en programas de Arquitectura y diseño", emoji: "🏛️" },
  { sentence: "Desarrollo de aplicaciones web", emoji: "🌐" },
];

const getRandomPhrases = (count = 5) => {
  const shuffled = [...phrases].sort(() => 0.5 - Math.random());
  return shuffled.slice(0, count);
};

let randomPhrase = getRandomPhrases();

setInterval(() => {
  randomPhrase = getRandomPhrases();
}, 1000);
---

<section class="py-16 bg-gradient-to-b from-[#0f2027] to-[#2c5364]">
  <div
    class="max-w-5xl mx-auto mb-10 p-10 bg-gradient-to-bl from-red-800 via-red-500 to-red-800 rounded-3xl shadow-2xl text-center"
  >
    <h1
      class="mb-4 text-5xl font-extrabold text-white bg-red-600/95 rounded-xl p-6 inline-block shadow-xl shadow-red-800/80 hover:bg-white hover:text-red-800 transition-all hover:shadow-2xl hover:shadow-red-400 hover:scale-110 duration-700"
    >
      ALEJANDRO DÍAZ OSORIO
    </h1>
    <p class="mb-6 text-xl font-semibold text-red-50 tracking-wide">
      Ingeniero de Sistemas y Telecomunicaciones
    </p>
  </div>

  <div
    class="grid md:grid-cols-2 gap-8 items-center justify-center max-w-5xl mx-auto p-10 bg-gradient-to-br from-red-800 via-red-500 to-red-800 rounded-3xl shadow-2xl"
  >
    <a
      href="https://www.linkedin.com/in/alejandro-diaz-osorio/"
      target="_blank"
      rel="noopener noreferrer"
      class="flex justify-center items-center"
      aria-label="LinkedIn profile"
    >
      <Image
        src={profile}
        alt="Foto de perfil de Alejandro Díaz Osorio"
        loading="eager"
        class="aspect-square size-52 rounded-full object-cover shadow-xl shadow-red-900 border-4 border-red-500 hover:scale-130 hover:shadow-2xl hover:shadow-red-400 hover:rotate-[-10deg] transition-all duration-700"
      />
    </a>
    <ul class="text-red-50 text-lg italic space-y-3 font-semibold text-left">
      {
        randomPhrase.map((phrase) => (
          <li>
            <span class="flex items-center gap-2 group hover:text-gray-300 hover:scale-120 transition-all duration-200">
              {phrase.emoji}
              <span class="group-hover:underline transition-all ">
                {phrase.sentence}
              </span>
            </span>
          </li>
        ))
      }
    </ul>
  </div>
</section>
